<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppExchance" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AppExchance" parent="Base.Theme.AppExchance" />

    <style name="TitleText" parent="Base.Theme.AppExchance">
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="Botoes" parent="Base.Theme.AppExchance">
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <style name="BotaoRignt" parent="Base.Theme.AppExchance">
        <item name="android:layout_alignParentRight">true</item>
        <item name="android:layout_margin">20dp</item>
        <item name="android:radius">5px</item>
    </style>

</resources>